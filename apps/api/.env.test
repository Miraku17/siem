# Local test database — never point this at a deployed environment.
# Started by `docker compose up -d postgres` from the repo root.
DATABASE_URL="postgresql://siem:siem@localhost:5432/siem_test"
DIRECT_URL="postgresql://siem:siem@localhost:5432/siem_test"
JWT_SECRET="test-secret-not-used-anywhere-real"
JWT_EXPIRES_IN="1d"
